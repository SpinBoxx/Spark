{% extends 'base.html.twig' %}

{% block title %}Modifier la question{% endblock %}

{% block body %}
    <h1>Modifier la question</h1>

    {{ include('faq_question/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('faq_question_index') }}">Retour</a>

    {{ include('faq_question/_delete_form.html.twig') }}
{% endblock %}
